# 出行翻译 · 后端对接说明（API 契约）

> 给后端同学：本小程序的**翻译 / 语音识别(ASR) / 语音合成(TTS) / 拍照OCR** 四个能力，
> 前端已写好调用与解析逻辑，全部收敛在 `services/` 目录。你们只需：
> 1. 提供下面 4 个 HTTPS 接口；
> 2. 在 `services/config.js` 填入 endpoint 与鉴权信息、把对应 `enabled` 改为 `true`；
> 3. 在小程序后台登记服务器域名。
>
> 未配置时前端自动走 `data/mock-data.js` 的演示数据，不影响联调前的运行。

---

## 0. 通用约定

| 项 | 说明 |
| --- | --- |
| 协议 | **HTTPS**（小程序强制），方法均为 **POST** |
| 域名登记 | 小程序后台 → 开发管理 → 开发设置 → 服务器域名：把接口域名加入 **request 合法域名** 和 **uploadFile 合法域名**；TTS 返回的音频域名加入 **downloadFile 合法域名** |
| 鉴权 | 由后端决定。前端可在 `services/config.js` 配 `appKey`/`appSecret`，并按需在各 `services/*.js` 的 `header` 或 `data` 里带上（例：`header: { Authorization: 'Bearer xxx' }`）。告诉我你们的鉴权方式，我来补这几行 |
| 编码 | 全部 UTF-8、JSON |
| 语言 code | `zh` 中文、`en` English、`ja` 日本語、`ko` 한국어、`fr` Français、`es` Español（见 `data/phrases.js`，可扩展） |
| 失败处理 | 非 2xx、或缺少约定字段时，前端提示"失败/重试"。建议失败也返回 200 + 错误体，或用标准 HTTP 错误码均可 |

---

## 1. 文本翻译 `POST /translate`

前端位置：`services/translate.js` → `translateText()`（`wx.request`）

**请求体（application/json）**
```json
{
  "q": "请问这附近有药店吗？",
  "from": "zh",
  "to": "ja"
}
```

**响应体（application/json）**
```json
{
  "translation": "この近くに薬局はありますか？"
}
```
- 前端取字段优先级：`translation` → `result` → `text`（任选其一返回即可，推荐 `translation`）。

---

## 2. 语音识别 ASR `POST /asr`

前端位置：`services/speech.js` → `recognizeAudio()`（`wx.uploadFile`，multipart/form-data）
> 仅当 `config.asr.enabled = true` 时，语音页才会真实录音并上传；否则走演示句子。

**请求（multipart/form-data）**
| 部分 | 内容 |
| --- | --- |
| 文件字段名 | `audio` |
| 音频格式 | mp3，采样率 16000，单声道（前端 `RecorderManager` 已按此录制） |
| formData | `{ "lang": "zh" }`（源语言 code） |

**响应（JSON 字符串，uploadFile 返回的是字符串，前端会 `JSON.parse`）**
```json
{
  "text": "请问这里可以帮我叫出租车吗？"
}
```
- 前端取字段优先级：`text` → `result`。
- 识别出的文本会再走 `/translate` 得到译文（语音页流程：录音 → ASR → 翻译 → TTS 朗读）。

---

## 3. 语音合成 TTS `POST /tts`

前端位置：`services/speech.js` → `playTTS()`（`wx.request`，拿到音频地址后用 `InnerAudioContext` 播放）

**请求体（application/json）**
```json
{
  "text": "この近くに薬局はありますか？",
  "lang": "ja"
}
```

**响应体（application/json）**
```json
{
  "audioUrl": "https://your-cdn.com/tts/abc123.mp3"
}
```
- 前端取字段优先级：`audioUrl` → `url`。
- 音频格式建议 **mp3/aac**；URL 域名需加入小程序 **downloadFile 合法域名**。
- 也可返回带签名的临时地址（有效期内可播放即可）。

---

## 4. 拍照 OCR + 翻译 `POST /ocr`

前端位置：`services/ocr.js` → `recognizeImage()`（`wx.uploadFile`，multipart/form-data）

**请求（multipart/form-data）**
| 部分 | 内容 |
| --- | --- |
| 文件字段名 | `image` |
| 图片 | 用户拍摄/相册选择的图片（jpg/png） |
| formData | `{ "from": "ja", "to": "zh" }`（from=图片上文字的语言，to=要翻译成的语言） |

**响应（JSON 字符串，前端会 `JSON.parse`）**
```json
{
  "results": [
    { "src": "主厨海鲜意面", "tgt": "Chef's seafood pasta", "note": "建议追问是否含贝类" },
    { "src": "含贝类", "tgt": "Contains shellfish", "note": "过敏人群请注意" }
  ]
}
```
- 前端取 `results` 数组，每项：`src`（识别原文，必填）、`tgt`（译文，必填）、`note`（补充说明，选填）。
- 多段文字就返回多个元素；无结果返回 `{ "results": [] }`。

---

## 5. 联调步骤

1. 后端部署好上述 4 个 HTTPS 接口。
2. 改 `services/config.js`：
   ```js
   translate: { enabled: true, endpoint: 'https://api.xxx.com/translate', appKey: '...' },
   asr:       { enabled: true, endpoint: 'https://api.xxx.com/asr',  appKey: '...' },
   tts:       { enabled: true, endpoint: 'https://api.xxx.com/tts',  appKey: '...' },
   ocr:       { enabled: true, endpoint: 'https://api.xxx.com/ocr',  appKey: '...' },
   ```
3. 小程序后台登记 request / uploadFile / downloadFile 合法域名。
4. 如鉴权字段需要加在 header 或 body，把方案告知前端（或直接改 `services/*.js` 里标了 `// TODO` 的那几行）。
5. 用微信开发者工具「真机调试」联调（相机/录音需真机）。

> 字段名若与你们现有接口不一致，两种处理：① 后端按本契约适配；② 告诉我你们的字段，我改前端解析。任选其一。
