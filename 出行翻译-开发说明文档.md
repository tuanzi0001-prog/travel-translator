# 出行翻译 · 开发说明文档

> 面向出国旅行的随身翻译小程序 · 蓝色玻璃拟态（Glassmorphism + Neumorphism）风格
> 配套原型文件：`v4-travel-translator.html`（单文件 HTML，可直接浏览器打开预览交互）

---

## 1. 产品概述

出行翻译是一款帮助用户在境外开口沟通的随身翻译工具，聚焦旅途中最高频、最怕开口的场景：问路、点餐、入住、海关、紧急求助。核心能力分为三种输入方式——文本翻译、语音对话、拍照翻译，并提供常用短语包与收藏功能。

原型默认演示语向为「中文 → 日本語」，正式版应支持多语言互译。

---

## 2. 信息架构与导航

底部导航共三个核心入口，以"语音"为中心凸起按钮（FAB）突出主功能；"我的"通过右上角头像进入，不占用底部导航。

| 入口 | 类型 | 进入方式 | 说明 |
| --- | --- | --- | --- |
| 文本 | 普通 tab | 底部左 | 首页，默认页 |
| 语音 | 中央 FAB（蓝色凸起） | 底部中 | 全屏沉浸式翻译 |
| 拍照 | 普通 tab | 底部右 | 全屏唤起相机 |
| 我的 | 头像入口 | 右上角头像 | 收藏与个人信息 |

导航规则：

- 进入「语音」「拍照」为全屏沉浸模式，自动隐藏顶部栏与底部导航，状态栏文字转白，提供左上角返回。
- 进入「我的」时，顶部左侧显示"返回"按钮、隐藏头像与欢迎语、标题左对齐；其余页面顶部右侧显示头像。
- 返回按钮仅在「我的」页出现，首页不出现。

---

## 3. 设计规范

### 3.1 颜色

| 变量 | 值 | 用途 |
| --- | --- | --- |
| `--ink` | `#1b2a4a` | 主文字（深蓝墨） |
| `--muted` | `#7488a8` | 次级文字 |
| `--blue` | `#4b8df8` | 主蓝 |
| `--blue-deep` | `#2f6fe0` | 深蓝（图标、强调字） |
| `--blue-grad` | `linear-gradient(135deg,#5b9bff,#3f7ef0)` | 主按钮 / FAB / 语音大屏 |
| `--blue-soft` | `#eaf2ff` | 浅蓝底（图标块、旗标） |
| `--coral` / `--coral-grad` | `#ff7a5c` / `渐变` | 暖色点缀（收藏选中、相机珊瑚等） |
| `--glass` / `--glass-strong` | `rgba(255,255,255,0.66 / 0.86)` | 磨砂玻璃卡片底 |

> 注：语音中央按钮已按需求统一为**蓝色**，不使用橙色。暖色仅用于收藏选中（♥）等小面积点缀。

### 3.2 圆角

- 卡片大圆角 `--radius: 22px`
- 中圆角 `--radius-sm: 16px`
- 胶囊 / 标签 `999px`
- 手机外壳 `40px`

### 3.3 阴影（新拟态柔和投影）

- `--soft-shadow: 0 10px 30px rgba(70,110,190,.12)`
- `--soft-shadow-lg: 0 24px 60px rgba(60,100,180,.18)`
- `--inner-light: inset 0 1px 0 rgba(255,255,255,.7)`（内高光，增强立体感）

### 3.4 字体与背景

- 字体族：Inter / PingFang SC / Microsoft YaHei 等系统字体回退。
- 全局背景为多层蓝色径向渐变叠加，营造通透柔和氛围。

---

## 4. 页面与组件

### 4.1 文本（首页）

结构：语言切换胶囊行 → 输入卡 → 常用短语区。

- **语言切换胶囊**：左右两个带国旗的玻璃胶囊，中间交换按钮（点击旋转 180°，正式版需真实交换语向并重译）。
- **输入卡**：多行输入框，**朗读原文按钮内嵌在输入框右下角**；下方为整行"翻译"主按钮。
- **译文结果区**：浅蓝底卡片，含译文、提示语、以及三个操作 chip：朗读译文 / 复制 / 收藏。
- **常用短语区**：四个分类 tab（机场海关 / 餐厅点餐 / 酒店入住 / 紧急求助），切换显示对应短语列表；每条短语含目标语言旗标、原文+译文、朗读、收藏。点短语正文会回填到输入框。

### 4.2 语音（沉浸式翻译大屏）

参考蓝色翻译 App 风格的全屏体验：

- 顶部：返回 + 语向胶囊（中文 → 日本語）。
- 中部：淡色显示原文，大字显示译文，上方状态文案（"点击说话 / 正在聆听 / 正在翻译 / 已翻译"）。
- 底部：20 条细线频谱波形（静止为高低错落造型，录音时整体跳动）+ 麦克风按钮，按钮外有同心圆光环（录音时一圈圈向外扩散脉冲）。
- 交互：点击麦克风开始/结束；结束后约 0.65s 出译文并自动 TTS 朗读；点译文可重听；演示内置多句循环。

### 4.3 拍照（全屏取景）

点"拍照"直接唤起相机，减少路径：

- 实时取景画面铺满，左上返回、右上"中文（简体）"胶囊。
- 底部：模式条（关闭解释 / AI 自动）+ 大号白色快门 + 相册按钮，上方黄色提示条。
- 交互：按快门 → 快门缩放动效 + 扫描线 → 约 1.1s 后识别结果卡从底部滑出（含每条结果的朗读、收藏、关闭）。

### 4.4 我的

- 顶部左侧返回。
- 个人资料卡（单一头像，避免重复）+ 三项统计（收藏数 / 翻译次数 / 到访国家）。
- 我的收藏列表：展示收藏的短语与译文，可朗读、可删除（垃圾桶图标）、可一键清空；无收藏时显示空状态。

---

## 5. 交互与动效清单

| 交互 | 触发 | 表现 |
| --- | --- | --- |
| 翻译加载 | 点翻译 | 按钮内三点跳动 → 约 0.75s 出结果 |
| 语音录音 | 点麦克风 | 波形跳动 + 同心圆脉冲 + 状态切换 |
| 语音出译文 | 结束录音 | 延迟出译文并 TTS 朗读 |
| 拍照识别 | 点快门 | 快门动效 + 扫描线 → 结果卡上滑 |
| 收藏 | 点 ♥ | 选中变珊瑚色，同步进「我的」，Toast 提示；译文区/短语/拍照结果三处状态联动 |
| 复制 | 点复制 | 调 clipboard，Toast 提示 |
| 语音合成（TTS） | 朗读按钮 | `speechSynthesis`，原文 `zh-CN`、译文 `ja-JP`，按钮高亮 |
| 页面切换 | tab/返回 | 淡入位移过渡；沉浸页切换隐藏顶/底栏 |
| Toast | 多处 | 底部居中黑底提示，约 1.7s 自动消失 |

---

## 6. 数据结构（原型内）

```js
// 常用短语包：分类 -> [[原文, 译文], ...]
phraseData = {
  机场海关: [...], 餐厅点餐: [...], 酒店入住: [...], 紧急求助: [...]
}

// 收藏：数组，去重以 src 为键
favorites = [ { src: "原文", tgt: "译文" }, ... ]

// 演示用翻译映射（正式版替换为翻译 API）
samples = Map<原文, 译文>
```

关键函数：`showScreen(name)` 切页、`runTranslate()` 翻译、`toggleFav(src,tgt)` 收藏、`makePhraseRow()` 渲染短语行、`renderFav()` 渲染收藏、`speak(text,lang,btn)` 朗读。

---

## 7. 待接入的真实能力（原型为模拟）

正式开发需把以下"演示逻辑"替换为真实服务：

- **翻译**：接入翻译 API，替换 `samples` 映射与 `getTranslation()`；支持多语向与语向交换重译。
- **语音识别（ASR）**：麦克风录音 → 实时转写，替换语音页内置循环句。
- **语音合成（TTS）**：当前用浏览器 `speechSynthesis`，正式版建议服务端 TTS 以保证音色与离线可控。
- **拍照 OCR**：调用 OCR + 翻译，替换固定识别结果；接入真实相机取景与相册选图。
- **收藏持久化**：当前仅内存（刷新即失，原型按规范未用浏览器存储）；正式版用本地存储 / 云端账号同步。
- **常用短语包**：建议做成可本地打开、离线可用；区分联网能力提示。

---

## 8. 验收清单

- [ ] 底部三导航 + 头像入"我的"，语音 FAB 为蓝色凸起。
- [ ] 返回按钮**仅**出现在「我的」页且位于左侧；首页无返回。
- [ ] 语音、拍照为全屏沉浸（隐藏顶/底栏，状态栏转白，有返回）。
- [ ] 输入框右下角朗读按钮；译文区有朗读/复制/收藏。
- [ ] 四类短语 tab 切换正常，每条可朗读、可收藏。
- [ ] 收藏在译文区 / 短语 / 拍照结果三处状态联动，并汇入「我的」。
- [ ] 各动效（加载点、波形、同心圆、快门扫描、Toast）表现正常。
- [ ] 多语向与真实翻译/ASR/TTS/OCR 接口已替换演示逻辑。

---

*本文档基于原型 `v4-travel-translator.html` 整理，作为开发参考。具体取值以原型 CSS `:root` 变量与脚本为准。*
